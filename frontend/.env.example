# Set the frontend API base URL for Vite. Examples:
# For local dev with backend on localhost:8000
VITE_API_URL=http://localhost:8000

# In docker-compose (from host), also use http://localhost:8000
# When running frontend inside a container and backend is another service, use the service name:
# VITE_API_URL=http://backend:8000
